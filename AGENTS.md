# AGENTS.md

## 1. 基本身份与优先级

- 你是本仓库的专业 AI 编码协作者，始终使用简体中文。
- 指令优先级：系统/开发者指令 > 用户当前任务 > 本文件 > 代码注释和惯例。
- 本文件约束适用于整个仓库；更近目录中的 `AGENTS.md` 优先，但不得违反更高优先级规则。
- 不确定、矛盾或存在安全/数据/兼容性风险时，暂停修改并提问；不要用猜测填空。

## 2. 任务执行流程

### 2.1 先定义目标

开始前明确：

1. 目标、非目标和用户可见变化。
2. 成功标准、影响范围和主要风险。
3. 不确定点及其可能解释；若会改变实现，先向用户确认。
4. 最小可行方案。若有更简单或更高效的实现，先说明理由。

复杂任务先给出简短计划，并为每一步标注验证方式；简单任务可直接执行。

### 2.2 先读后改

- 先运行 `git status --short`，再用 `rg` 查找入口、调用方、类型、配置、协议和测试。
- 阅读受影响的实现、读写双方、错误路径和相关文档；不得基于局部代码猜测全局行为。
- 工作区已有改动默认属于用户或其他协作者：不还原、删除、覆盖、暂存或提交无关改动。

### 2.3 最小实现

- 只修改完成目标所必需的文件和代码；保留无关代码、原有注释和编码习惯。
- 优先复用现有 API、校验、鉴权、状态和错误处理；不用一次性需求搭建新抽象层或复杂架构。
- 不盲目增加未来扩展性、配置项或极低概率容错；发现本次修改产生的无效导入、变量和代码可删除。
- 发现原有死代码、重复逻辑或相邻问题，只在交付中提醒，除非用户明确要求处理。
- 每次修改用户可见功能、交互、协议或配置后，检查是否需要同步补充或更新新人指引（client/src/lib/userGuide.ts）；若需要，必须同时更新对应步骤、锚点或功能使用标记，并在验证中说明。

### 2.4 验证闭环

- 修复：先用测试或可重复场景复现，再修改并回归。
- 新增功能或校验：覆盖正常、异常、未认证和越权路径；涉及并发时覆盖重复提交、重连和失败恢复。
- 验证失败时继续定位和修复；不得把未运行、失败或无法覆盖的检查写成“已验证”。
- 临时创建的功能测试文件或其他 `test` 文件仅用于本次验证；测试完成后必须删除，不得遗留在工作区或提交到仓库。
- 完成标准是验收条件全部满足，而不是代码已经改动。

## 3. 项目边界

OpenMusic 是包含 Web、Node.js 服务端、Redis、Socket.IO、外部音乐服务、管理后台和 Flutter 移动端的实时点歌系统。变更需同时考虑正确性、安全性、兼容性、可回退性和可观测性。

| 路径 | 约束 |
|---|---|
| `client/` | React + Vite + TypeScript；页面在 `src/pages/`，状态在 `src/stores/`，通用能力在 `src/lib/`。普通用户主站不得引入 Ant Design。 |
| `server/` | Express + Socket.IO；`index.js` 是入口；生产环境 Redis 必需，不得以改变生产语义的内存兜底替代。 |
| `mobile/` | Flutter WebView 与 Android 媒体能力；`mobile/WEB_SHELL.md` 是 Web、Flutter、Android 的桥接契约。 |
| `docs/`、`deploy/`、Docker 配置 | 用户可见功能、协议、配置、端口、卷、健康检查和部署语义变更需同步评估。 |
| `data/`、运行时 JSON、`.env` | 运行时状态、密钥和用户数据；禁止提交或当作源码修改。 |

## 4. 不可破坏的契约

### 接口、权限与数据

- 服务端最终裁决鉴权、权限、限流、输入校验、数据完整性和敏感操作；前端隐藏或禁用控件不是安全措施。
- HTTP、Socket.IO 和移动端桥接的事件名、字段、错误语义属于公共协议。修改前搜索全部发送方和监听方；优先只追加字段，并保持旧客户端兼容或安全降级。
- 所有外部输入均不可信。新增或修改接口必须明确认证、授权、输入限制、限流、状态码、稳定错误码和脱敏边界。
- Redis 键名、TTL、序列化格式和索引属于数据模型；变更必须支持旧数据读取、幂等迁移、失败恢复和回滚。
- 房间、播放、队列、收藏、成员和聊天是并发状态；考虑竞态、重复提交、消息乱序、断线重连和服务重启。

### 安全与外部依赖

- 密钥、Cookie、Token、管理员凭据和用户身份信息仅在服务端按最小范围使用，不得进入前端、URL、日志、错误响应或提交记录。
- 不得通过关闭 HTTPS 校验、授权、CSRF、签名、IP 限制、限流或异常处理来解决联调问题。
- 对外请求、代理、上传和重定向必须限制协议、目标、超时、大小、类型和响应，阻止回环、内网、链路本地及云元数据访问。
- AI 输出不能绕过既有权限、限流、审计和参数校验；模型不可用时基础房间功能仍需可用，且不发送不必要的敏感信息。

### 前端依赖边界

- `antd` 与 `@ant-design/*` 仅允许在 `client/src/pages/admin/`、`client/src/pages/Admin.tsx`、`client/src/pages/Setup.tsx` 使用。
- 普通用户页面优先使用原生 HTML、现有轻量组件或 `lucide-react`；新增依赖前评估首屏加载、代码分割和构建产物。
- 新增悬浮提示必须复用项目统一的 Tooltip 组件：队列区域使用 `QueueTip`，其他界面使用 `Tooltip`；禁止新增原生 `title` 作为交互提示。

## 5. 按风险验证

- 文档或规则：`git diff --check`，核对路径、命令和事实。
- 前端：`npm run typecheck`、`npm run build:check`；UI 需检查桌面和窄屏的加载、空态、失败态及关键流程。
- 服务端：`npm test --prefix server`，并按需执行语法检查、`GET /api/health`、关键 HTTP/Socket 场景。
- 协议、配置、Redis、部署或移动端：核对所有读写双方、旧客户端/旧数据、权限、重连、失败恢复和回滚。
- 不因通过检查而削弱类型检查、权限、CORS、HTTPS、限流或 CI 门禁。

## 6. 交付格式

完成后用简体中文说明以下内容；没有则写“无”：

1. **变更摘要**：改了什么，用户或系统行为如何变化。
2. **影响范围**：客户端、服务端、移动端、数据、配置、部署和文档。
3. **验证结果**：实际执行的命令、关键场景和结果。
4. **风险与未验证项**：未覆盖的环境、依赖、兼容性假设或剩余风险。
5. **上线与回滚**：配置、迁移、发布顺序、观测信号和回退方式；无则写“无”。

## 7. 禁止事项

- 不提交 `.env`、真实密钥、Cookie、OAuth 会话、用户数据、运行时 JSON、日志、构建产物、`node_modules/` 或 `data/`。
- 不使用 `git reset --hard`、`git checkout --` 或批量删除覆盖现有工作，除非用户明确授权。
- 不通过吞掉异常、删除测试、放宽边界检查或隐藏 import 来消除报错。

## 8. 发布与专项门禁

### 8.1 身份、OAuth 与回调

- API Key、Cookie、OAuth token/client secret、Redis 密码和管理员凭据只能在服务端使用；日志和响应必须脱敏。
- 不得关闭 HTTPS 校验、签名、会话校验、CSRF 或 OAuth `state` 校验来解决联调问题。
- 生产环境的 `CLIENT_ID_SECRET`、房间凭证密钥必须稳定持久；禁止启动时自动轮换已有密钥。
- 管理员入口 gate、登录、会话探测和 OAuth 状态接口不得泄露后台是否启用、真实入口或绑定账号信息。

### 8.2 Socket、移动端与旧客户端

- 每个 Socket 事件都要核对发起端、服务端鉴权、payload 校验、ack、广播范围、幂等、重连和旧客户端兼容。
- 房间私密信息、管理员信息、凭据和内部错误不得广播给无关房间成员。
- `mobile/WEB_SHELL.md` 中的字段和命令名是 Web、Flutter、Android 公共契约；涉及播放、队列、聊天、房间或登录时必须核对三端调用方。
- 原生媒体桥接只提交请求，最终权限判断仍由网页和服务端执行；桥接不可用时基础网页功能必须继续可用。

### 8.3 Redis、部署与回滚

- Redis 是生产房间、收藏、凭据、公告、封禁和管理数据的必需依赖；键名、TTL、序列化和索引变化必须提供旧数据兼容、幂等迁移、失败恢复和回滚步骤。
- 修改 Docker、Nginx、端口、卷、环境变量或启动顺序时，需说明重启需求、持久化影响、健康检查和恢复方式。
- 协议或配置变更遵循“先兼容服务端、再升级客户端”；删除旧字段或行为需经过兼容窗口。
- 发布记录至少包含版本、变更摘要、配置/迁移、验收结果、回滚触发条件和负责人。

### 8.4 CI 与可观测性

- CI 继续执行前端 `npm run build:check`、服务端语法检查和 `npm test`；不得通过削弱检查使构建通过。
- 关键功能至少记录成功量、失败量、拒绝/限流量、上游错误或延迟；日志不得被描述为完整监控或告警系统。
- 未实现依赖深度健康检查时，`GET /api/health` 只能描述进程可响应，不得虚构 Redis 或上游健康状态。
